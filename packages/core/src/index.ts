/**
 * @testvibe/core — point d'entrée du package.
 *
 * Ce package est volontairement vide de logique métier à ce stade
 * (Phase 0 — bootstrap). La modélisation du domaine (Person, Union,
 * Filiation, Event) sera ajoutée en Phase 1 (tâche #20).
 */

export const CORE_PACKAGE_NAME = "@testvibe/core";
