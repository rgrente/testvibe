# testvibe

Dépôt bac à sable pour valider le workflow du codeur Hermes.

## Tests

Exécuter la suite de tests (bibliothèque standard uniquement, aucune dépendance) :

```bash
python -m unittest -v
```
